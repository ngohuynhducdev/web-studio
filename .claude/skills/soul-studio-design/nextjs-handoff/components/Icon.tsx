/**
 * Re-export of lucide-react's icon set as the canonical SOUL STUDIO icon system.
 * Stroke: 1.5–1.75. Color: currentColor.
 *
 *   import { Menu, ArrowUpRight } from '@/components/Icon';
 *   <Menu size={20} strokeWidth={1.75} />
 *
 * Never use emoji. Never invent SVGs.
 */
export {
  Menu, X,
  ArrowUpRight, ArrowRight, ChevronRight, ChevronDown,
  Check, Plus, Minus,
  Calendar, Clock, User, Heart, Star,
  Phone, Mail, MapPin, Send,
  Instagram, Facebook,
} from 'lucide-react';
